# RTN Video Capture Requirements
selenium==4.15.0
pyautogui==0.9.54
opencv-python==4.8.1.78
pytesseract==0.3.10
pillow==10.1.0
numpy==1.24.3

# Make sure to also install:
# brew install tesseract (on macOS)
# brew install --cask chromedriver (on macOS)